# LightsOutSolver

A React + TypeScript Lights Out puzzle with a built-in solver.

## Local Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## GitHub Pages

The Vite base path is configured for `/LightsOutSolver/`. Pushing to `main` deploys the `dist` build through GitHub Actions.
