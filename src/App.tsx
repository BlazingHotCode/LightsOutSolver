export function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Lights Out Solver</p>
        <h1>Build, play, and solve Lights Out puzzles.</h1>
        <p className="lede">
          Toggle tiles on the board, then ask the solver for the shortest set of
          presses it finds.
        </p>
      </section>
    </main>
  );
}
